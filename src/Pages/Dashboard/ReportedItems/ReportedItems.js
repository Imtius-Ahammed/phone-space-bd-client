import React from 'react';
import Title from '../../../hooks/Title';

const ReportedItems = () => {
  Title('Reported Items')
  return (
    <div>
      <h1>This is Reported Items</h1>
    </div>
  );
};

export default ReportedItems;